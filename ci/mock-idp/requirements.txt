# Standalone deps for the dev/e2e mock IdP (ci/mock-idp). Intentionally
# NOT part of the uv workspace — it is a throwaway container, loosely pinned.
# TEST ONLY, NEVER PRODUCTION.
fastapi==0.141.1
uvicorn>=0.30
PyJWT[crypto]==2.14.0
python-multipart==0.0.32
